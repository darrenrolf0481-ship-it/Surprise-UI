# Resources Directory

This directory contains all non-code assets, configurations, schemas, and external manifests required by the TCS-AGI Framework.

Subdirectories:
- config/: General environment configuration and build files.
- data/: Manifests pointing to external data sources.
- schema/: JSON files defining input/output data structures and validation rules.
